"DroneAI"
{
	"min_attack_range"  "250.0" // Minimum distance we try to keep between our target
	"max_attack_range"  "1100.0" // Maximum distance we will engage our target at
	"detection_range"   "2000.0" // Distance at which we can find targets

	"max_idle_height"   "400.0" // While idle, how high we will travel from our current position when moving
	"max_combat_height" "300.0" // While in combat, how high we will travel from our current position
	"min_height"        "200.0" // Minimum height we will hover at from the ground
	"level_combat_height"   "1"
	"pursue_target"		"0"		// Do we pursue targets when they leave our sight
	
	"max_move_range"    "1200.0" // Maximum distance we will travel when moving to a new position
	"min_move_range"    "800.0" // Minimum distance we will try to travel when moving

	"think_rate"        "0.1" // How many seconds must pass before we process subsequent think requests
	"idle_timer"        "2.0" // How many seconds must pass before we process idle requests
	"combat_timer"      "4.0" // How many seconds must pass before we process combat requests
	"pursue_timer"      "6.0" // How long we will pursue lost targets for
	"burst_time"        "0.5" // How long we hold down the trigger for when attacking. 0 = continuously held down
	"burst_delay"       "4.0" // Time between bursts when attacking
	"combat_move_time"	"5.0" // How many seconds must pass before we process move requests while in combat
	"aim_speed"			"300.0" // How quickly we turn our aim towards our target
}
